#include <stdio.h>

int main(void) {
    int sum = 0, i = 0;
    int data[50] = "helloworldo";

    while(i < 100) {
        sum += data[i], ++i;
    }

    int e(i, sum);

    int j = (f1(), f2());

    //x = (y = 3, (z = ++y + 2) + 5);

    for (int i = 0, j = 100; i != 10; ++i, j -= 10);

    printf("jason"),
    printf("fedin"),
    printf("done");

    int houseprice = 500;

    houseprice = (249, 500);

    char ch, date;
    //printf("%d:", x, y);

    return 0;
}